# (excluido del workspace)

Este directorio quedó fuera del alcance de Fase 3 — Foundation (ver `docs/fase-3-foundation.md`). El árbol de packages acordado en esta fase solo incluye `apps/api` y `apps/cli`.

Está excluido explícitamente en `pnpm-workspace.yaml` (`!apps/web`), así que no participa en `pnpm install`, `build`, `typecheck`, `lint` ni `test`. Puedes borrar esta carpeta con seguridad cuando quieras.
