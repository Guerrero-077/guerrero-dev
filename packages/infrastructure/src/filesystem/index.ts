/**
 * infrastructure/filesystem — placeholder de Fase 3.
 *
 * Acceso a archivos del proyecto (lectura, listado, watch) usado por
 * project-intelligence y por el ExecutionEngine para aplicar cambios.
 * Sin implementación real todavía.
 */
export const PACKAGE_AREA = "filesystem";
