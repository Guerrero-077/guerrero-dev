/**
 * infrastructure/execution — placeholder de Fase 3.
 *
 * Reservado para utilidades de infraestructura de bajo nivel usadas por
 * los ExecutionEngine adapters (sandboxing, gestión de procesos), NO para
 * los adapters en sí — esos (ClineExecutionEngine, OpenCodeExecutionEngine)
 * viven en `@guerrero-dev/execution`, que implementa el puerto
 * `IExecutionEngine` de `application` (Fase 3.8, Fase 7).
 */
export const PACKAGE_AREA = "execution";
